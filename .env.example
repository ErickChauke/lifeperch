DATABASE_URL=
# Neon unpooled (direct) connection string. Used by prisma migrate on deploy.
DIRECT_URL=
NEXTAUTH_SECRET=
NEXTAUTH_URL=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
VAULT_PIN=
# Resend API key for the daily todo digest. Unset disables sending (logs a no-op).
RESEND_API_KEY=
# Shared secret the cron route checks. Vercel Cron sends it as a Bearer token.
CRON_SECRET=
# Optional branded sender. Defaults to onboarding@resend.dev when unset.
EMAIL_FROM=
